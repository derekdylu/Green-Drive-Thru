# Frontend

The frontend is a React 18 single-page application built with Vite. See the repository-level [README](../README.md) for setup, test, build, project structure, licensing, and security information.

```bash
npm ci
npm run dev
```
